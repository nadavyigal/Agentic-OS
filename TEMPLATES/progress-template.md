# Project Progress

> Copy this to `tasks/progress.md` in a project repo and keep the keyed block at the top.
> The Agentic OS refresh parser reads these keys; see `STATUS-SCHEMA.md` for the contract.
> Keep `Last Updated` and `Last Validation` current so the project can hold High confidence.

Project: <Project Name>
Status: <In Progress | Active | Pre-release | Blocked>
Current Phase: <one-line headline state shown on the dashboard>
Active Story: <what is being worked on right now>
Last Completed Story: <most recent finished story, with id/date if useful>
Next Recommended Story: <the single next action; drives the dashboard Next Action>
Estimated Completion: <free text or percent, e.g. 68%>
Blockers: <semicolon-separated; use — if none>
Risks: <semicolon-separated; use — if none>
Last Validation: <what was built/tested, the result, and the date — use evidence words like "passed" / "succeeded"; see STATUS-SCHEMA.md vocabulary>
Last Updated: <YYYY-MM-DD>
Latest QA Report: <path to a QA doc, or —>

<!--
Notes below the keyed block are free-form and ignored by the parser. Use them for context,
tables, wiring notes, etc. Lines starting with #, |, -, >, or * never interfere with parsing.
-->
